function settingsSkeletonBuilder() {
    const settings = {
        username: null,
        email: null,
        isTipEnabled: true,
        isExportReportEnabled: true
    }

    return settings;
}

export default settingsSkeletonBuilder;