{
  "name": "sparc",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "npm run build-client && docker compose up --build",
    "start": "docker compose up",
    "server": "npm run start --prefix server",
    "client": "npm run start --prefix client",
    "dev": "concurrently \"npm run dev-server\" \"npm run client\"",
    "dev-server": "npm run dev --prefix server",
    "bootstrap": "npm install && npm install --prefix server && npm install --prefix client",
    "build-client": "npm install --prefix client && npm run build --prefix client"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "node-cron": "^3.0.3",
    "concurrently": "^9.0.1"
  }
}